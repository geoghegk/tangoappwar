# Generated by IBM TransformationAdvisor
# Thu Jun 13 13:14:32 IST 2019

apiVersion: v1
kind: Service
metadata:
  name: tangoappwar
  labels:
    app: tangoappwar
spec:
  ports:
    - port: 9080
  selector:
    app: tangoappwar
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: tangoappwar
  name: tangoappwar
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tangoappwar
    spec:
      containers:
      - image: mycluster.icp:8500/default/tangoappwar:latest
        name: tangoappwar
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
